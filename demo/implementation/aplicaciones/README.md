# Aplicaciones destino — implementación específica de la demo

Implementación específica de la versión demo para cada aplicación destino. Cada aplicación vive en una subcarpeta propia.

Las simplificaciones respecto del sistema general (`implementation/aplicaciones/`) se documentan en los ADRs `DI-XXXX` de cada subcarpeta.