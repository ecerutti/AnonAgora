# Aplicaciones destino — diseño específico de la demo

Diseño específico de la versión demo para cada aplicación destino. Cada aplicación vive en una subcarpeta propia.

Las simplificaciones respecto del sistema general (`design/aplicaciones/`) se documentan en los ADRs `DP-XXXX` de cada subcarpeta.