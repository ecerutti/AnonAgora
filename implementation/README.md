# Implementation

Esta carpeta contiene la implementación técnica de la plataforma.

Las decisiones de implementación se documentan en `implementation/adr/`.

Las decisiones sobre qué hace el sistema y por qué viven en `design/adr/` y deben leerse antes de tomar o entender cualquier decisión de implementación.
